
/* eslint-disable @typescript-eslint/no-unused-vars */

// Tailwind config is expected to be present in the root of the project.
// In case it isn’t, create a simple one like:
// module.exports = { content: ['./src/**/*.{ts,tsx}'], theme: {}, plugins: [] };
