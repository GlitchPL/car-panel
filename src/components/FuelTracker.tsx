import React from 'react';

const FuelTracker: React.FC = () => {
  return (
    <div className="p-4">
      <h2 className="text-xl font-semibold mb-4">Fuel Tracker (placeholder)</h2>
      <p>Do tego miejsca możesz dodać interaktywny tracker paliwa.</p>
    </div>
  );
};

export default FuelTracker;
